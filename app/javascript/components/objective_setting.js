const objectiveSetting = () => {
    const profilePage = document.querySelector("container-user");
    if (profilePage) {
      const subBtn = document.querySelector(".btn-left");
      subBtn.addEventListener("click", (event) => {

      });

      const addBtn = document.querySelector(".btn-right");
      addBtn.addEventListener("click", (event) => {

      });

  }
}

export { objectiveSetting };
